services:
  # mongo:
  #   image: mongo:7
  #   restart: unless-stopped
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongo_data:/data/db

  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      MONGODB_URI: mongodb+srv://noman:noman1212@cluster0.hjpl2n8.mongodb.net/netguard_ai?retryWrites=true&w=majority&appName=Cluster0
      MONGODB_DB: netguard_ai
      BLOCK_API_KEY: ${BLOCK_API_KEY:-change-me-in-production}
      CORS_ORIGINS: http://localhost:5173,http://127.0.0.1:5173,http://localhost:8080,http://127.0.0.1:8080
      WHITELIST_IPS: 127.0.0.1,::1,mongo,api
      SMTP_HOST: smtp-relay.brevo.com
      SMTP_PORT: "587"
      SMTP_USER: b84a35001@smtp-brevo.com
      SMTP_PASSWORD: xsmtpsib-0e29144f3b50debcbc4563fe08f968a6dab151e2bd514da69eddb6202b7363ca-iomVuLeUgJ8t5PaM
      SMTP_FROM: nomanmughal2796@gmail.com
      ALERT_CONFIDENCE_THRESHOLD: "0.35"
    volumes:
      - api_uploads:/app/uploads

  web:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        VITE_API_BASE: ""
        VITE_BLOCK_API_KEY: ${BLOCK_API_KEY:-change-me-in-production}
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - api

volumes:
  mongo_data:
  api_uploads:
